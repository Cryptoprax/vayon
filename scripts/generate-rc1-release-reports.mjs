import { readFile, readdir, writeFile } from "node:fs/promises";
import { join, relative } from "node:path";

const root = process.cwd(), docs = join(root, "docs"), app = join(root, "app"), features = join(root, "features");
const walk = async (directory) => (await Promise.all((await readdir(directory, { withFileTypes: true })).map(async (entry) => entry.isDirectory() ? walk(join(directory, entry.name)) : [join(directory, entry.name)]))).flat();
const normalize = (file) => relative(root, file).replaceAll("\\", "/");
const appFiles = await walk(app), featureFiles = await walk(features);
const byName = (name) => appFiles.filter((file) => file.endsWith(`/${name}`) || file.endsWith(`\\${name}`)).map(normalize).sort();
const bySegment = (segment) => featureFiles.filter((file) => normalize(file).includes(`/${segment}/`)).map(normalize).sort();
const pages = byName("page.tsx"), layouts = byName("layout.tsx"), apis = byName("route.ts"), errors = byName("error.tsx");
const providers = bySegment("providers"), repositories = bySegment("repositories"), services = bySegment("services"), components = bySegment("components");
const webhooks = apis.filter((file) => file.includes("/webhooks/")), aiFiles = featureFiles.map(normalize).filter((file) => /(?:^|\/)(?:ai|ai-|intelligence|creative-generation|property-matching)/i.test(file) || /\/ai-/i.test(file));
const catalog = JSON.parse(await readFile(join(root, "supabase", ".temp", "sprint89-production-catalog.json"), "utf8"));
if (catalog.projectRef !== "aanonopiylqpfvpoqvdc" || catalog.readOnly !== true) throw new Error("RC1 fails closed without the verified read-only production catalog.");
const schema = catalog.schema;
const section = (title, values) => `## ${title} (${values.length})\n\n${values.length ? values.map((value) => `- \`${value}\``).join("\n") : "None discovered."}`;
const inventory = `# RC1 platform inventory\n\nGenerated ${new Date().toISOString()} from source files and the sanitized read-only production catalog. Build-generated dynamic paths are reported separately by the production build.\n\n## Summary\n\n| Surface | Count |\n| --- | ---: |\n| App Router pages | ${pages.length} |\n| Layouts | ${layouts.length} |\n| API route handlers | ${apis.length} |\n| Route error boundaries | ${errors.length} |\n| Components | ${components.length} |\n| Providers | ${providers.length} |\n| Repositories | ${repositories.length} |\n| Services | ${services.length} |\n| AI-related source files | ${aiFiles.length} |\n| Production public tables | ${schema.tables.filter((x) => x.schema_name === "public").length} |\n| Production functions | ${schema.functions.length} |\n| Production triggers | ${schema.triggers.length} |\n| Production policies | ${schema.policies.length} |\n| Production buckets | ${schema.buckets.length} |\n\n${[section("Pages", pages), section("Layouts", layouts), section("API routes", apis), section("Webhook routes", webhooks), section("Error boundaries", errors), section("Providers", providers), section("Repositories", repositories), section("Services", services), section("Client and server components", components), section("Production tables", schema.tables.filter((x) => x.schema_name === "public").map((x) => `public.${x.name}`)), section("Production functions and RPC candidates", schema.functions.map((x) => `${x.schema_name}.${x.name}(${x.arguments})`)), section("Production triggers", schema.triggers.map((x) => `${x.schema_name}.${x.table_name}.${x.name}`)), section("Production RLS policies", schema.policies.map((x) => `${x.schema_name}.${x.table_name}.${x.name}`)), section("Production storage buckets", schema.buckets.map((x) => `storage.${x.id}`))].join("\n\n")}\n\n## Runtime-only inventory\n\nBackground processing is implemented through API processors, workflow runtime services, database claim RPCs, notification/email queues, and webhook handlers listed above. Live queue depth, provider account state, browser console output, hydration, and navigation require production runtime access and are not inferred from source.\n`;
await writeFile(join(docs, "PLATFORM_INVENTORY.md"), inventory);

const modules = [
  ["Authentication","READY","Core production tables exist; provider journeys require live browser evidence."], ["Organizations","DEGRADED","Core organizations/workspaces exist; enterprise Sprint 51/76 objects need the database patch."], ["Workspaces","READY","Core workspace and membership schema exists."], ["CRM","READY","Core CRM schema is represented in production."], ["Properties","DEGRADED","Core properties exist; enterprise inventory/site-visit extensions need the patch."], ["Leads","READY","Core lead schema exists."], ["Deals","READY","Core deal schema exists."], ["Meetings","READY","Core meetings schema exists."], ["Calendar","DEGRADED","Local scheduling works; Google provider requires flag, credentials, consent, and health evidence."], ["Tasks","READY","Core task schema exists."], ["Communications","DEGRADED","Core communications exist; live providers require credentials and connection evidence."], ["Marketing Studio","BLOCKED","Code and entitlement are ready; creative/growth production tables are missing."], ["Creative Studio","BLOCKED","Creative schema and generation queues require the Version 1 patch."], ["Growth Studio","BLOCKED","Growth campaign schema requires the Version 1 patch."], ["Landing Pages","DEGRADED","Draft generator is code-ready; publishing is intentionally unavailable."], ["Campaign Builder","BLOCKED","Campaign persistence requires the Version 1 patch."], ["AI Workforce","DEGRADED","Runtime schema exists; OpenAI health observed billing_required during build."], ["AI Employees","DEGRADED","Configured employees exist; live inference depends on OpenAI billing/health."], ["Knowledge","BLOCKED","Recovery is safe, but all Knowledge production tables/RPCs/bucket are missing."], ["Help Center","DEGRADED","Static documentation remains available; tenant retrieval needs Knowledge schema."], ["Product Intelligence","BLOCKED","Sprint 86.4 tables/RPCs are missing."], ["Continuous Learning","BLOCKED","Sprint 86.5 tables/RPCs are missing."], ["Notifications","DEGRADED","Core notification tables exist; Sprint 59 platform is only partially represented."], ["Billing","BLOCKED","Core subscription records exist; commercial Sprint 50/83 schema and live provider evidence are missing."], ["Subscriptions","DEGRADED","Core subscription schema exists; checkout/portal require live Stripe configuration."], ["Settings","READY","Routes compile and retain authenticated governance."], ["Integrations","DEGRADED","Provider-neutral center is ready; connections depend on flags, credentials, and consent."], ["Founder Dashboard","DEGRADED","Code compiles; authenticated runtime verification is pending."], ["Demo Environment","READY","Aurora demo is isolated, deterministic, and read-only."], ["Public Website","READY","Public routes, SEO assets, contact resilience, and build pass."], ["Pricing","DEGRADED","Page is ready; live checkout requires billing provider configuration."], ["Contact Sales / Book Demo","DEGRADED","Failure isolation is tested; production submission requires marketing tables/RPCs."],
];
const moduleTable = modules.map(([name,status,reason]) => `| ${name} | ${status} | ${reason} |`).join("\n");
await writeFile(join(docs, "MODULE_STATUS.md"), `# RC1 module status\n\n| Module | Status | Evidence / blocker |\n| --- | --- | --- |\n${moduleTable}\n\nREADY means repository/build evidence and required production core schema exist. DEGRADED means safe fallback or partial operation is available. BLOCKED means a required production database/provider dependency is absent.\n`);

const flags = [["vayon_intelligence","Production Ready","Enabled by default; explicit false is the emergency kill switch."],["ai","Production Ready / dependency-bound","Enable only with healthy OpenAI billing and provider validation."],["gmail","Production Ready / dependency-bound","Enable only with Google credentials and incremental consent."],["whatsapp","Production Ready / dependency-bound","Enable only with Meta credentials and verified webhook."],["stripe","Production Ready / dependency-bound","Enable only with live credentials, prices, and webhook verification."],["google_identity","Production Ready / dependency-bound","Explicit configuration required."],["google_calendar","Production Ready / dependency-bound","Explicit configuration and consent required."],["google_drive","Production Ready / dependency-bound","Explicit configuration and consent required."],["google_contacts","Production Ready / dependency-bound","Explicit configuration and consent required."],["microsoft_identity","Production Ready / dependency-bound","Explicit Entra configuration required."],["workflow_runtime","Production Ready / database-bound","Keep disabled until workflow schema patch and post-deploy verification pass."],["beta","Experimental","Disabled unless deliberately enabled in a non-production experiment."]];
await writeFile(join(docs, "FEATURE_STATUS.md"), `# RC1 feature status\n\n| Flag | Classification | RC1 activation decision |\n| --- | --- | --- |\n${flags.map((x) => `| ${x[0]} | ${x[1]} | ${x[2]} |`).join("\n")}\n\nMarketing Studio is plan-licensed and has no beta flag. Referrals, affiliates, autonomous AI, live social publishing, and video rendering remain unactivated. No credential-dependent flag was changed without proven infrastructure.\n`);

const missing = catalog.migrations.filter((x) => x.status === "missing" || x.status === "partially represented");
await writeFile(join(docs, "DATABASE_STATUS.md"), `# RC1 production database status\n\nProject: \`${catalog.projectRef}\`  \nInspection: read-only  \nMigration history: absent  \nDecision: **BLOCKING — Version 1 patch not yet applied**\n\n| Surface | EXISTS | MISSING / BLOCKING |\n| --- | ---: | ---: |\n| Tables | ${catalog.counts.tables} | 90 required Version 1 public tables |\n| Columns | ${catalog.counts.columns} | 69 required Version 1 columns |\n| Indexes | ${catalog.counts.indexes} | 87 required Version 1 indexes |\n| Functions/RPCs | ${catalog.counts.functions} | 101 required Version 1 functions |\n| Triggers | ${catalog.counts.triggers} | 7 required Version 1 triggers |\n| Policies | ${catalog.counts.policies} | 78 directly cataloged Version 1 policies plus generated policy families |\n| Storage buckets | ${catalog.counts.buckets} | \`knowledge-documents\`, \`product-feedback\` |\n| Views | ${catalog.counts.views} | None required by catalog comparison |\n| Realtime publications | ${catalog.counts.publicationTables} | OPTIONAL unless a reviewed module requires realtime |\n\n## Missing or partial migration units (${missing.length})\n\n${missing.map((x) => `- **${x.status.toUpperCase()}** \`${x.file}\` (${x.present}/${x.expected} catalog objects present)`).join("\n")}\n\nRequired deployment artifacts: \`supabase/reconciliation/VERSION1_PRODUCTION_PATCH.sql\` and \`VERSION1_POST_DEPLOY_CHECK.sql\`. Do not replay history or run db push.\n`);

await writeFile(join(docs, "INTEGRATION_STATUS.md"), `# RC1 integration status\n\n| Integration | Code | Live evidence | Status |\n| --- | --- | --- | --- |\n| OpenAI | Provider and health diagnostics compile | Build observed \`billing_required / credit_balance_exhausted\` | BLOCKED |\n| Stripe | Checkout, portal, subscriptions, invoices, webhooks | Live credentials/prices/webhook evidence unavailable | BLOCKED |\n| Razorpay | Provider and signed webhook architecture | Live credentials/webhook evidence unavailable | BLOCKED |\n| Supabase | Authenticated repository architecture | Read-only schema catalog verified; Version 1 schema behind | DEGRADED |\n| Gmail | OAuth/provider/repository implemented | Credentials, consent, connection health unverified | DEGRADED |\n| Google Calendar | OAuth/provider/repository implemented | Credentials, consent, connection health unverified | DEGRADED |\n| Google Drive/Contacts | Incremental consent architecture implemented | Live connection unverified | DEGRADED |\n| Microsoft 365 | Entra/provider-neutral architecture implemented | Live tenant/consent unverified | DEGRADED |\n| WhatsApp | Cloud API and signed webhook implemented | Credentials and end-to-end webhook delivery unverified | DEGRADED |\n| Email | Provider-neutral Resend/SendGrid/Postmark/SMTP architecture | Active provider and DNS authentication unverified | BLOCKED |\n| Monitoring | Sentry/PostHog extension points | Production DSN/project evidence unavailable | BLOCKED |\n`);

const ready = modules.filter((x) => x[1] === "READY").length, degraded = modules.filter((x) => x[1] === "DEGRADED").length, blocked = modules.filter((x) => x[1] === "BLOCKED").length, score = Math.round((ready + degraded * 0.5) / modules.length * 100);
await writeFile(join(docs, "PRODUCTION_READINESS_SCORE.md"), `# RC1 production readiness score\n\n## ${score}/100 — NO-GO\n\n| Classification | Modules | Weight |\n| --- | ---: | ---: |\n| READY | ${ready} | 100% |\n| DEGRADED | ${degraded} | 50% |\n| BLOCKED | ${blocked} | 0% |\n\nThe score is module-weighted and intentionally conservative. It does not award production credit for code-only provider integrations. Launch remains blocked by the Version 1 database patch, OpenAI billing, commercial payment configuration, email/provider configuration, monitoring, backups/restore evidence, and authenticated browser acceptance testing.\n`);

await writeFile(join(docs, "VERSION1_RELEASE_REPORT.md"), `# VAYON Version 1 RC1 release report\n\nDecision: **NO-GO for public commercial launch**. Repository validation is strong, but production infrastructure is not synchronized or fully evidenced.\n\n## Inventory\n\n- Source App Router pages: ${pages.length}\n- Source API handlers: ${apis.length}\n- Source layouts: ${layouts.length}\n- Build-expanded routes/pages: verified by \`npm run build\` (334 generated pages in the latest run)\n- Audited modules: ${modules.length}\n- AI-related source files: ${aiFiles.length}\n\n## Verification boundaries\n\nStatic compilation, TypeScript, ESLint, regression, route-source, theme, UX, security architecture, performance artifacts, activation, and read-only database inspection are automated locally. Real authenticated browser traversal, hydration/console checks, Core Web Vitals, cross-browser accessibility, and state-changing founder acceptance journeys were **not executed** because no approved RC1 test tenant, browser session, provider credentials, or authorization to mutate production was supplied. They remain launch blockers and are never represented as passing.\n\n## Founder acceptance status\n\nOrganization/member/workspace creation, property/lead/campaign creation, AI creative generation, landing-page generation, meeting/task/proposal creation, subscription upgrade, Knowledge search, Workforce, Marketing, Creative, and Growth journeys are **BLOCKED pending an isolated staging tenant plus the Version 1 schema patch and provider configuration**. The Aurora demo validates presentation only and is not substituted for production acceptance.\n\n## Activation\n\nVAYON Intelligence is consistently enabled by default with an explicit emergency disable. Marketing remains plan-licensed without beta gating. Credential- and database-dependent flags remain fail-closed. Experimental beta, referrals, affiliates, autonomous execution, live social publishing, and video rendering remain disabled or Preview.\n\n## Required sign-off\n\n1. Apply the reviewed Version 1 patch to a restored production clone, run post-deploy checks, then deploy under the runbook.\n2. Resolve OpenAI billing and verify live Responses API health.\n3. Configure and verify Stripe/Razorpay, email, Google, Microsoft, WhatsApp, monitoring, DNS email authentication, backups, and alerts.\n4. Run authenticated founder journeys and every route in Chrome, Edge, Safari, and Firefox with console, network, accessibility, responsive, and performance capture.\n5. Re-run RC1 reports and require zero BLOCKED modules before launch.\n`);

console.log(JSON.stringify({ pages: pages.length, apis: apis.length, layouts: layouts.length, modules: modules.length, aiFiles: aiFiles.length, ready, degraded, blocked, readinessScore: score }, null, 2));
