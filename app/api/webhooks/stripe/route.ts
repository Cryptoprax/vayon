import{NextResponse}from"next/server";import{StripeService}from"@/features/vayon/billing/services/stripe.service";import{StripeWebhookSignatureError}from"@/features/vayon/billing/providers/stripe.provider";export async function POST(request:Request){const signature=request.headers.get("stripe-signature");if(!signature)return NextResponse.json({error:"Missing signature"},{status:400});try{await new StripeService().webhook(await request.text(),signature);return NextResponse.json({received:true})}catch(reason){if(reason instanceof StripeWebhookSignatureError)return NextResponse.json({error:"Invalid webhook signature"},{status:400});return NextResponse.json({error:"Webhook processing failed"},{status:500})}}
