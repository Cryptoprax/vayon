import { ButtonLink } from "@/features/platform/design-system";
import { WorkspaceContent, WorkspaceHeader } from "@/features/platform/design-system/layout/WorkspaceLayouts";
import { MembersManagement } from "@/features/platform/organization/components/OrganizationUI";
import { EnterpriseOrganizationService } from "@/features/platform/organization";
import { enforcePagePermission } from "@/features/platform/permissions/runtime/http";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){const query=await searchParams;await enforcePagePermission("team_management");const snapshot=await new EnterpriseOrganizationService().snapshot();return <WorkspaceContent ><WorkspaceHeader title="Team Members" description="Bring your team together and keep everyone?s access up to date." actions={snapshot.canManage && <ButtonLink href="#invite-team">Invite Team</ButtonLink>}/>{query.error && <p role="alert" className="mt-4 rounded-xl bg-vds-danger-soft p-3 text-sm">We could not complete that request. Check your access, the email address, and pending invitations before trying again.</p>}{query.success && <p role="status" className="mt-4 rounded-xl bg-vds-success-soft p-3 text-sm">Your team request was completed. Review the member and invitation lists below.</p>}<MembersManagement snapshot={snapshot}/></WorkspaceContent>}
