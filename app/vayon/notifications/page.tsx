import{NotificationCenter}from"@/features/notifications/components/NotificationCenter";import{NotificationService}from"@/features/notifications/services/notification.service";import{notificationCategories,type NotificationCategory,type NotificationView}from"@/features/notifications/types";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const p=await searchParams;const view=validView(first(p.view))?first(p.view)as NotificationView:"all",category=notificationCategories.includes(first(p.category)as NotificationCategory)?first(p.category)as NotificationCategory:undefined,search=first(p.q).slice(0,100),page=Math.max(1,Number(first(p.page))||1);const service=new NotificationService(),[result,metrics]=await Promise.all([service.list({view,category,search,page}),service.observability()]);return <main className="mx-auto max-w-7xl px-4 py-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-vds-primary">Enterprise collaboration</p><h1 className="mt-1 text-3xl font-semibold">Enterprise Inbox</h1><p className="mt-2 text-sm text-vds-muted">One tenant-scoped inbox for AI work, approvals, assignments, mentions, CRM replies, tasks, meetings and operational events. Every item opens its existing source workflow.</p><NotificationCenter result={result} query={{view,category,search,page}} metrics={metrics}/></main>}
const first=(value:string|string[]|undefined)=>Array.isArray(value)?value[0]??"":value??"";const validView=(value:string):value is NotificationView=>["all","unread","read","archived","snoozed","starred","mentioned","high_priority"].includes(value);
