import { CompanyForm } from "@/features/vayon/crm-company/CompanyForm";import { createCompanyAction } from "@/features/vayon/crm-company/actions";import { CrmCompanyService } from "@/features/vayon/crm-company/service";import { CrmShell } from "@/features/vayon/crm-engine/components/CrmShell";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams,service=await CrmCompanyService.production();return <CrmShell title="Create Company" description="Add an agency, builder, or company and connect its people and properties."><CompanyForm action={createCompanyAction} owners={await service.salespeople()} error={q.error}/></CrmShell>}
