import { notFound } from "next/navigation";import { CompanyForm } from "@/features/vayon/crm-company/CompanyForm";import { updateCompanyAction } from "@/features/vayon/crm-company/actions";import { CrmCompanyService } from "@/features/vayon/crm-company/service";import { CrmShell } from "@/features/vayon/crm-engine/components/CrmShell";
export default async function Page({params,searchParams}:{params:Promise<{companyId:string}>;searchParams:Promise<{error?:string}>}){const id=(await params).companyId,q=await searchParams,service=await CrmCompanyService.production(),[model,owners]=await Promise.all([service.detail(id),service.salespeople()]);if(!model)notFound();return <CrmShell title="Edit Company" description="Update the authoritative company record without changing its relationships."><CompanyForm action={updateCompanyAction.bind(null,id)} company={model.company} owners={owners} error={q.error}/></CrmShell>}
