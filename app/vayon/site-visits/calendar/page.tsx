import { WorkspaceContent } from "@/features/platform/design-system/layout/WorkspaceLayouts";
import{VisitCalendar,VisitHeader}from"@/features/vayon/site-visits/VisitViews";import{SiteVisitLifecycleService}from"@/features/vayon/site-visits/service";
export default async function Page({searchParams}:{searchParams:Promise<{view?:string}>}){const{view="week"}=await searchParams,service=await SiteVisitLifecycleService.production(),snapshot=await service.snapshot();return <WorkspaceContent ><VisitHeader title="Visit calendar" description="Plan day, week, month, and agenda schedules with working-hour, overlap, workload, and travel-buffer awareness."/><VisitCalendar snapshot={snapshot} view={view}/></WorkspaceContent>}
