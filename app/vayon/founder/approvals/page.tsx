import{AuthenticationService}from"@/features/authentication/services/authentication.service";import{AICollaborationService}from"@/features/platform/ai-collaboration";import{FounderApprovalCenter}from"@/features/vayon/founder-approval-center/FounderApprovalCenter";import{WorkforceService}from"@/features/vayon/operational-workforce/services/workforce.service";
export default async function Page(){const[data,workforce,user]=await Promise.all([(await AICollaborationService.production()).dashboard(),(await WorkforceService.production()).snapshot(),new AuthenticationService().user()]),name=String(user?.user_metadata?.full_name??user?.user_metadata?.name??user?.email?.split("@")[0]??"Founder").split(" ")[0];return <FounderApprovalCenter data={data} workforce={workforce} founderName={name}/>}
