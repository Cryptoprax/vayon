import { WorkspaceContent, WorkspaceHeader } from "@/features/platform/design-system/layout/WorkspaceLayouts";
import{notFound}from"next/navigation";import{updateDealAction}from"@/features/vayon/deal/actions/deal.actions";import{DealForm}from"@/features/vayon/deal/components/DealForm";import{DealService}from"@/features/vayon/deal/services/deal.service";export default async function Page({params,searchParams}:{params:Promise<{dealId:string}>;searchParams:Promise<{error?:string}>}){const id=(await params).dealId,deal=await new DealService().detail(id);if(!deal)notFound();const q=await searchParams;return <WorkspaceContent ><WorkspaceHeader><h1 className="mb-7 text-3xl font-semibold">Edit deal</h1></WorkspaceHeader><DealForm deal={deal} error={q.error} action={updateDealAction.bind(null,id)}/></WorkspaceContent>}
