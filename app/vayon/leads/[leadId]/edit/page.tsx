import{notFound}from"next/navigation";import{updateLeadAction}from"@/features/vayon/lead/actions/lead.actions";import{LeadWizard}from"@/features/vayon/lead/components/LeadWizard";import{LeadService}from"@/features/vayon/lead/services/lead.service";import{CrmCompanyService}from"@/features/vayon/crm-company/service";import{PropertyService}from"@/features/vayon/property/services/property.service";export default async function Page({params,searchParams}:{params:Promise<{leadId:string}>;searchParams:Promise<{error?:string}>}){const id=(await params).leadId,[lead,salespeople,properties]=await Promise.all([new LeadService().detail(id),(await CrmCompanyService.production()).salespeople(),new PropertyService().list({page:1,pageSize:100,view:"grid"})]);if(!lead)notFound();const q=await searchParams;return <main className="mx-auto max-w-4xl px-5 py-8"><h1 className="mb-7 text-3xl font-semibold">Edit lead</h1><LeadWizard lead={lead} error={q.error} action={updateLeadAction.bind(null,id)} salespeople={salespeople} properties={properties.items}/></main>}
