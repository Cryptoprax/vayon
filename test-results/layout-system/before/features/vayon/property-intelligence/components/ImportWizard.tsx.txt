"use client";import{DatabaseZap,FileImage,FileSpreadsheet,FileText,Globe2,ImageIcon,Link2,Mic,RotateCcw,UploadCloud}from"lucide-react";import{useRouter}from"next/navigation";import{useRef,useState}from"react";import{Button}from"@/features/platform/design-system";import{PropertyImportService}from"../services/property-import.service";import type{ImportMethod,ImportPreview,ImportStage}from"../types";import{ImportProgress}from"./ImportProgress";import{MediaManager}from"./MediaManager";const methods=[{method:"manual",label:"Manual Entry",description:"Use the governed property form.",icon:FileText},{method:"csv",label:"CSV",description:"Map and validate spreadsheet rows.",icon:FileSpreadsheet},{method:"xlsx",label:"Excel (.xlsx)",description:"Workbook extraction pipeline.",icon:FileSpreadsheet,badge:"Preview"},{method:"pdf",label:"PDF Brochure",description:"OCR-ready brochure workflow.",icon:FileImage,badge:"Future OCR"},{method:"images",label:"Property Images",description:"Vision-ready media ingestion.",icon:ImageIcon,badge:"Future Vision"},{method:"url",label:"Property URL",description:"Official provider integrations only.",icon:Globe2,badge:"No scraping"},{method:"voice",label:"Voice Note",description:"Transcription pipeline placeholder.",icon:Mic,badge:"Placeholder"},{method:"crm",label:"CRM Export",description:"Adapter-ready migration workflow.",icon:DatabaseZap,badge:"Preview"}]as const;export function ImportWizard(){const router=useRouter(),file=useRef<HTMLInputElement>(null),[method,setMethod]=useState<ImportMethod>("csv"),[stage,setStage]=useState<ImportStage>("idle"),[progress,setProgress]=useState(0),[preview,setPreview]=useState<ImportPreview>(),[url,setUrl]=useState("");async function process(selected?:File){try{setStage("reading");setProgress(5);const source={method,name:selected?.name??(url||method),file:selected,url:url||undefined},result=await new PropertyImportService().preview(source,setProgress);setPreview(result);setStage("preview");setProgress(100)}catch(error){setStage("error");setProgress(100);setPreview(undefined);console.error(error)}}return <div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{methods.map(item=><Button variant="control" key={item.method} type="button" onClick={()=>{if(item.method==="manual")router.push("/vayon/properties/new");else{setMethod(item.method);setPreview(undefined);setStage("idle")}}} aria-pressed={method===item.method} className={`group focus-ring min-h-40 rounded-2xl border p-4 text-left hover:-translate-y-1 ${method===item.method?"border-vds-accent-border bg-vds-primary/[.07]":"border-vds-border/[.075] bg-vds-surface/[.025]"}`}><div className="flex justify-between"><span className="grid size-10 place-items-center rounded-2xl bg-vds-primary/[.08] text-vds-primary"><item.icon className="size-5"/></span>{"badge"in item&&<span className="text-[9px] uppercase tracking-wider text-vds-subtle">{item.badge}</span>}</div><p className="mt-5 text-sm font-medium">{item.label}</p><p className="mt-1 text-xs leading-5 text-vds-subtle">{item.description}</p></Button>)}</div><section className="mt-5 rounded-3xl border border-vds-border/[.075] bg-vds-surface/[.025] p-5 sm:p-6">{method==="url"?<div><label className="text-sm font-medium">Official listing URL</label><div className="mt-2 flex gap-2"><div className="relative flex-1"><Link2 className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-vds-muted"/><input type="url" value={url} onChange={event=>setUrl(event.target.value)} placeholder="https://approved-provider.example/listing" className="h-11 w-full rounded-xl border border-vds-border bg-vds-input pl-10 pr-3 text-sm outline-none focus:border-vds-accent-border"/></div><Button onClick={()=>void process()} disabled={!url}>Prepare</Button></div><p className="mt-2 text-xs text-vds-subtle">Vayon does not fetch or scrape this URL. An official provider must be connected.</p></div>:method==="images"?<MediaManager/>:<Button variant="control" type="button" onClick={()=>file.current?.click()} onDragOver={event=>event.preventDefault()} onDrop={event=>{event.preventDefault();void process(event.dataTransfer.files[0])}} className="grid min-h-44 w-full place-items-center rounded-2xl border border-dashed border-vds-border-strong bg-vds-input text-center hover:border-vds-accent-border"><span><UploadCloud className="mx-auto size-7 text-vds-primary"/><span className="mt-3 block text-sm font-medium">Drop {method.toUpperCase()} source here</span><span className="mt-1 block text-xs text-vds-subtle">or choose a local file for safe preview</span></span></Button>}<input ref={file} type="file" className="sr-only" accept={method==="csv"?".csv,text/csv":method==="xlsx"?".xlsx":method==="pdf"?".pdf":undefined} onChange={event=>void process(event.target.files?.[0])}/>{stage!=="idle"&&<div className="mt-4"><ImportProgress stage={stage} progress={progress} message={stage==="error"?"Unable to preview source":stage==="preview"?"Preview ready":"Preparing import preview"}/></div>}{preview&&<Preview preview={preview}/>}</section></div>}
function Preview({preview}:{preview:ImportPreview}){const service=new PropertyImportService(),summary=service.validate(preview);return <div className="mt-5"><div className="flex flex-wrap gap-2 text-xs"><span className="rounded-full bg-vds-success-soft px-3 py-1 text-vds-success">{summary.valid.length} valid</span><span className="rounded-full bg-vds-danger-soft px-3 py-1 text-vds-danger">{summary.invalid.length} invalid</span><span className="rounded-full bg-vds-warning-soft px-3 py-1 text-vds-warning">{summary.duplicates.length} duplicates</span></div>{preview.warnings.map(warning=><p key={warning} className="mt-3 rounded-xl border border-vds-warning bg-vds-warning/[.05] p-3 text-xs text-vds-warning">{warning}</p>)}{preview.columns.length>0&&<div className="mt-4 overflow-x-auto rounded-2xl border border-vds-border/[.07]"><table className="w-full min-w-[650px] text-left text-xs"><thead className="bg-vds-surface/[.04] text-vds-muted"><tr>{preview.columns.map(column=><th className="p-3" key={column.source}>{column.source}<span className="block text-vds-primary">→ {column.target} · {Math.round(column.confidence*100)}%</span></th>)}</tr></thead><tbody>{preview.rows.slice(0,8).map(row=><tr key={row.index} className={`border-t border-vds-border/[.06] ${!row.valid?"bg-vds-danger/[.03]":""}`}>{preview.columns.map(column=><td className="max-w-40 truncate p-3" key={column.source}>{row.values[column.source]||"—"}</td>)}</tr>)}</tbody></table></div>}<div className="mt-4 flex items-center justify-between"><Button variant="control" type="button" className="flex items-center gap-2 text-xs text-vds-muted"><RotateCcw className="size-4"/>Undo import</Button><p className="text-xs text-vds-subtle">Preview only · no records written</p></div></div>}
