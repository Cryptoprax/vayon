"use client";

import { useRef, useState } from "react";
import { Button } from "@/features/platform/design-system";
import { OnboardingCsvImportService } from "../services/csv-import.service";
import type { ImportKind, ImportPreview } from "../domain/enterprise-onboarding";

const providers = ["HubSpot", "Salesforce", "Zoho", "Pipedrive"] as const;
const kinds: readonly ImportKind[] = ["contacts", "companies", "leads", "deals", "properties"];

export function DataImportWorkspace() {
  const input = useRef<HTMLInputElement>(null);
  const [kind, setKind] = useState<ImportKind>("contacts"), [preview, setPreview] = useState<ImportPreview>(), [error, setError] = useState("");
  async function previewFile(file?: File) {
    if (!file) return;
    try { setPreview(new OnboardingCsvImportService().preview(kind, await file.text())); setError(""); }
    catch { setPreview(undefined); setError("This CSV could not be read. Confirm that it is a valid UTF-8 comma-separated file."); }
  }
  return <div className="mt-8 space-y-6"><section className="rounded-2xl border border-vds-border bg-vds-surface p-5" aria-labelledby="csv-import-title"><h2 id="csv-import-title" className="text-xl font-semibold">CSV Import</h2><p className="mt-2 text-sm text-vds-muted">Choose a record type, validate required columns and review up to 100 rows before continuing with the existing governed import workflow.</p><div className="mt-5 grid gap-3 sm:grid-cols-[14rem_1fr]"><label className="text-sm font-medium">Record type<select value={kind} onChange={(event) => { setKind(event.target.value as ImportKind); setPreview(undefined); }} className="mt-2 block h-11 w-full rounded-xl border border-vds-border bg-vds-input px-3">{kinds.map((item) => <option value={item} key={item}>{item[0]?.toUpperCase()}{item.slice(1)}</option>)}</select></label><div className="rounded-xl border border-dashed border-vds-border p-4"><input ref={input} className="sr-only" type="file" accept=".csv,text/csv" onChange={(event) => void previewFile(event.target.files?.[0])}/><Button type="button" onClick={() => input.current?.click()}>Choose CSV file</Button><p className="mt-2 text-xs text-vds-muted">The file is parsed locally for validation and duplicate detection.</p></div></div>{error && <p className="mt-4 text-sm text-vds-danger" role="alert">{error}</p>}{preview && <div className="mt-5" role="status"><div className="grid gap-3 sm:grid-cols-3"><Metric label="Rows previewed" value={preview.rows.length}/><Metric label="Duplicates" value={preview.duplicates}/><Metric label="Validation errors" value={preview.errors.length}/></div>{preview.errors.length ? <ul className="mt-4 rounded-xl bg-vds-danger-soft p-4 text-sm text-vds-danger">{preview.errors.map((item) => <li key={item}>{item}</li>)}</ul> : <p className="mt-4 rounded-xl bg-vds-success-soft p-4 text-sm text-vds-success">CSV validated successfully and is ready for the governed import workflow.</p>}<div className="mt-4 overflow-x-auto"><table className="min-w-full text-left text-xs"><thead><tr>{preview.headers.map((header) => <th className="border-b border-vds-border p-2" key={header}>{header}</th>)}</tr></thead><tbody>{preview.rows.slice(0, 10).map((row, index) => <tr key={index}>{preview.headers.map((header) => <td className="border-b border-vds-border p-2" key={header}>{row[header]}</td>)}</tr>)}</tbody></table></div></div>}</section><section aria-labelledby="crm-connectors-title"><h2 id="crm-connectors-title" className="text-xl font-semibold">CRM connectors</h2><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{providers.map((provider) => <article className="rounded-2xl border border-vds-border bg-vds-surface p-5 opacity-75" key={provider}><div className="flex items-start justify-between gap-2"><h3 className="font-semibold">{provider}</h3><span className="rounded-full border border-vds-border px-2 py-1 text-[10px] font-semibold uppercase tracking-wide text-vds-muted">Coming Soon</span></div><p className="mt-3 text-sm text-vds-muted">Direct {provider} migration is not available yet.</p><Button className="mt-4" type="button" disabled>Unavailable</Button></article>)}</div></section></div>;
}
function Metric({ label, value }: { label: string; value: number }) { return <div className="rounded-xl bg-vds-elevated p-3"><p className="text-xs text-vds-muted">{label}</p><p className="mt-1 text-xl font-semibold">{value}</p></div>; }
