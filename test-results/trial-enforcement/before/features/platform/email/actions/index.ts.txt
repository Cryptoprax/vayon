"use server";import{revalidatePath}from"next/cache";import{EmailService}from"../services/email.service";const refresh=()=>{for(const path of["/vayon/settings/email","/vayon/settings/email/templates","/vayon/settings/email/history","/vayon/settings/email/queue"])revalidatePath(path)};export async function retryEmailAction(form:FormData){await new EmailService().retry(String(form.get("messageId")??""));refresh()}export async function saveEmailTemplateAction(form:FormData){await new EmailService().saveTemplate({key:String(form.get("key")??""),name:String(form.get("name")??""),locale:String(form.get("locale")??"en"),subject:String(form.get("subject")??""),html:String(form.get("html")??""),text:String(form.get("text")??""),active:form.get("active")==="on"});refresh()}
