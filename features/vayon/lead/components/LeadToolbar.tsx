import { WorkspaceFilters } from "@/features/platform/design-system/layout/WorkspaceLayouts";
import { Button } from "@/features/platform/design-system";
import{leadPriorities,leadSources,leadStatuses}from"../config/catalogs";export function LeadToolbar({query}:{query:Record<string,string|undefined>}){return <WorkspaceFilters><form><input name="search" defaultValue={query.search} aria-label="Quick search leads" placeholder="Search name, phone, email…" className="h-11 rounded-xl border border-vds-border bg-vds-input px-4"/>{[[leadStatuses,"status","All statuses"],[leadPriorities,"priority","All priorities"],[leadSources,"source","All sources"]].map(([items,name,label])=><select key={String(name)} name={String(name)} defaultValue={query[String(name)]} className="rounded-xl border border-vds-border bg-[var(--vds-color-surface)] px-3" aria-label={String(label)}><option value="">{String(label)}</option>{(items as typeof leadStatuses).map(x=><option key={x.code} value={x.code}>{x.label.default}</option>)}</select>)}<div className="md:col-span-4 flex flex-wrap gap-2"><Button variant="control" className="rounded-full bg-vds-hover px-4 py-2 text-xs">Apply filters</Button></div></form></WorkspaceFilters>}
