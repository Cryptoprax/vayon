import type{WorkforceEmployee}from"../domain/models";import{employeeIdentity}from"../identity/employee-identities";
export function EmployeeIdentityPanel({item}:{readonly item:WorkforceEmployee}){const identity=employeeIdentity(item.code,item);return <section className="rounded-[2rem] border border-vds-border bg-vds-surface/90 p-6 backdrop-blur sm:p-8" aria-labelledby="identity-title"><div className="flex flex-col gap-6 lg:flex-row lg:items-start"><span className={`grid size-24 shrink-0 place-items-center rounded-[2rem] ${identity.colorClass} text-2xl font-semibold text-vds-background shadow-xl`} aria-label={`${identity.name} avatar`}>{item.avatar}</span><div className="min-w-0 flex-1"><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Identity · {identity.color}</p><h2 id="identity-title" className="mt-2 text-3xl font-semibold">{identity.name} · {identity.role}</h2><p className="mt-4 max-w-3xl text-lg leading-8 text-vds-secondary">“{identity.introduction}”</p><p className="mt-4 max-w-3xl text-sm leading-6 text-vds-muted">{identity.biography}</p></div></div><div className="mt-7 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Field label="Mission" value={identity.mission}/><Field label="Working Style" value={identity.workingStyle}/><Field label="Communication Style" value={identity.communication.join(" · ")}/><Field label="Professional Motto" value={identity.motto}/></div><div className="mt-6 flex flex-wrap gap-2" aria-label="Employee strengths and badges">{[...identity.badges,...identity.strengths].map(value=><span className="rounded-full border border-vds-accent-border bg-vds-primary-soft px-3 py-1.5 text-xs font-medium text-vds-primary" key={value}>{value}</span>)}</div><p className="mt-6 text-xs text-vds-muted"><b>Personality Traits:</b> {identity.traits.join(" · ")} · <b>Current Focus:</b> {item.currentQueue?`${item.currentQueue} queued assignments`:"Waiting for the first assignment"}</p></section>}
function Field({label,value}:{label:string;value:string}){return <div className="rounded-2xl bg-vds-elevated p-4"><p className="text-xs text-vds-muted">{label}</p><p className="mt-2 text-sm font-medium">{value}</p></div>}
