import Link from "next/link";
import { CalendarDays, MapPin, Phone, Route, SquareCheckBig } from "lucide-react";
import type { CalendarItem } from "../types";
const icons={meeting:CalendarDays,visit:MapPin,call:Phone,task:SquareCheckBig};
export function CalendarWidget({items}:{items:CalendarItem[]}){return <section className="rounded-3xl border border-vds-border/[0.075] bg-vds-surface/[0.025] p-5 sm:p-6" aria-labelledby="calendar-heading"><div className="flex items-center justify-between"><div><p className="text-xs font-medium uppercase tracking-[.18em] text-vds-primary">Appointments · Viewings · Follow-ups · Calls · Meetings · Registrations · Deadlines</p><h2 id="calendar-heading" className="mt-2 text-xl font-semibold">Today&apos;s Agenda</h2></div><Link href="/vayon/calendar" className="text-xs text-vds-muted hover:text-vds-primary">Full calendar →</Link></div><div className="mt-5 divide-y divide-vds-divider/[0.06]">{items.slice(0,8).map((item)=>{const Icon=icons[item.kind];return <Link href={item.href} key={`${item.kind}-${item.id}`} className="focus-ring flex items-center gap-3 rounded-xl py-3 hover:px-2 hover:bg-vds-surface/[0.025]"><time className="w-16 shrink-0 text-xs font-medium text-vds-primary">{new Date(item.startsAt).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"})}</time><span className="grid size-8 shrink-0 place-items-center rounded-xl bg-vds-surface/[0.05] text-vds-secondary"><Icon className="size-4" aria-hidden="true"/></span><span className="min-w-0 flex-1"><span className="block truncate text-sm text-vds-secondary">{item.title}</span><span className="block truncate text-xs capitalize text-vds-subtle">{item.meta}</span></span></Link>})}{!items.length&&<div className="py-10 text-center"><Route className="mx-auto size-6 text-vds-subtle"/><p className="mt-3 text-sm font-medium text-vds-secondary">No agenda items today.</p><p className="mt-1 text-xs text-vds-muted">Schedule a property viewing or follow-up.</p></div>}</div></section>}
