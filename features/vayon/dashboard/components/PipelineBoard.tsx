import Link from "next/link";
import { ArrowUpRight, TrendingDown, TrendingUp } from "lucide-react";
import type { PipelineColumn } from "../types";

export function PipelineBoard({items,currency}:{items:PipelineColumn[];currency:string}){const format=new Intl.NumberFormat("en",{style:"currency",currency,notation:"compact",maximumFractionDigits:1});return <section className="rounded-3xl border border-vds-border/[0.075] bg-vds-surface/[0.025] p-5 sm:p-6" aria-labelledby="pipeline-heading"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-medium uppercase tracking-[.18em] text-vds-primary">Sales velocity</p><h2 id="pipeline-heading" className="mt-2 text-xl font-semibold">Sales Pipeline</h2></div><Link href="/vayon/deals" className="flex items-center gap-1 text-xs text-vds-muted hover:text-vds-primary">View all <ArrowUpRight className="size-4"/></Link></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4 2xl:grid-cols-8">{items.map((item,index)=><Link href={item.href} key={item.id} className="group focus-ring relative min-w-0 overflow-hidden rounded-2xl border border-vds-border/[0.07] bg-vds-input p-4 hover:-translate-y-0.5 hover:border-vds-accent-border hover:bg-vds-primary/[0.03]"><span className="absolute inset-x-0 top-0 h-0.5 bg-gradient-to-r from-vds-primary/30 to-vds-accent-soft" style={{opacity:.35+index*.08}}/><div className="flex items-start justify-between gap-2"><p className="text-sm font-medium text-vds-secondary">{item.label}</p><span className="rounded-full bg-vds-surface/[0.06] px-2 py-0.5 text-xs text-vds-muted">{item.count}</span></div><p className="mt-5 truncate text-lg font-semibold">{format.format(item.value)}</p><p className={`mt-2 flex items-center gap-1 text-xs ${item.trend>=0?"text-vds-success":"text-vds-danger"}`}>{item.trend>=0?<TrendingUp className="size-3"/>:<TrendingDown className="size-3"/>}{Math.abs(item.trend)}% this week</p><p className="mt-3 text-[11px] text-vds-subtle">Owner · Unavailable</p><span className="mt-3 block text-[11px] text-vds-subtle group-hover:text-vds-primary">Filter deals →</span></Link>)}</div></section>}
