import type { CompanyProfile } from "./domain";
export function CompanyMetrics({model}:{model:CompanyProfile}){const currency=model.company.currency,money=(value:number)=>new Intl.NumberFormat("en",{style:"currency",currency,maximumFractionDigits:0}).format(value),m=model.metrics,items=[["Total Leads",m.totalLeads],["Active Deals",m.activeDeals],["Won Deals",m.wonDeals],["Lost Deals",m.lostDeals],["Properties Sold",m.propertiesSold],["Revenue",money(model.revenue)],["Average Deal Size",money(m.averageDealSize)],["Sales Velocity",`${m.salesVelocityDays} days`],["Conversion",`${m.conversion.toFixed(1)}%`],["Assigned Agents",m.assignedAgents]];return <section aria-label="Company performance" className="mb-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{items.map(([label,value])=><article key={label} className="rounded-2xl border border-vds-border bg-vds-surface p-5"><p className="text-xs text-vds-muted">{label}</p><p className="mt-2 text-xl font-semibold">{value}</p></article>)}</section>}
