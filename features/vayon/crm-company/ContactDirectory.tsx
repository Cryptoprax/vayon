import Link from "next/link";import type { ContactRecord } from "./domain";import { SmartEmptyState } from "@/features/vayon/components/SmartEmptyState";
export function ContactDirectory({items}:{items:readonly ContactRecord[]}){return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.length?items.map(item=><Link key={item.id} href={`/vayon/crm/contacts/${item.id}`} className="vds-focus vds-card-motion rounded-2xl border border-vds-border bg-vds-surface p-5 shadow-sm hover:border-vds-accent-border motion-reduce:transition-none"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-full bg-vds-primary-soft font-semibold text-vds-primary">{item.name.slice(0,2).toUpperCase()}</span><div className="min-w-0"><h2 className="truncate font-semibold">{item.name}</h2><p className="truncate text-xs text-vds-muted">{item.position||item.relationship}</p></div></div><dl className="mt-5 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-vds-muted">Intent</dt><dd className="mt-1 font-medium">Not captured</dd></div><div><dt className="text-vds-muted">Budget</dt><dd className="mt-1 font-medium">Not captured</dd></div><div><dt className="text-vds-muted">Timeline</dt><dd className="mt-1 font-medium">Not captured</dd></div><div><dt className="text-vds-muted">Interest</dt><dd className="mt-1 font-medium">Not captured</dd></div><div><dt className="text-vds-muted">Assigned Agent</dt><dd className="mt-1 font-medium">{item.ownerName}</dd></div><div><dt className="text-vds-muted">Next Follow-up</dt><dd className="mt-1 font-medium">Not scheduled</dd></div><div><dt className="text-vds-muted">Recommended Property</dt><dd className="mt-1 font-medium">Unavailable</dd></div><div><dt className="text-vds-muted">AI Confidence</dt><dd className="mt-1 font-medium">Unavailable</dd></div></dl><p className="mt-4 border-t border-vds-divider pt-3 text-xs text-vds-muted">{item.companyName} · Updated {new Date(item.updatedAt).toLocaleDateString()}</p></Link>):<SmartEmptyState className="col-span-full" title="No contacts yet" description="Create a contact or import an existing client list to begin matching properties and planning follow-ups." primaryLabel="Create Contact" primaryHref="/vayon/leads/new" secondaryActions={[{label:"Import CSV",href:"/onboarding?setup=crm"}]} aiSuggestion="Start with active buyer enquiries so VAYON can recommend matching properties."/>}</div>}
