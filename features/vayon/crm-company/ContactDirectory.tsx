import Link from "next/link";import type { ContactRecord } from "./domain";import { SmartEmptyState } from "@/features/vayon/components/SmartEmptyState";
export function ContactDirectory({items}:{items:readonly ContactRecord[]}){return <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.length?items.map(item=><Link key={item.id} href={`/vayon/crm/contacts/${item.id}`} className="vds-focus rounded-2xl border border-vds-border bg-vds-surface p-5"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-full bg-vds-primary-soft font-semibold text-vds-primary">{item.name.slice(0,2).toUpperCase()}</span><div><h2 className="font-semibold">{item.name}</h2><p className="text-xs text-vds-muted">{item.position||item.relationship}</p></div></div><dl className="mt-5 grid grid-cols-2 gap-3 text-xs"><div><dt className="text-vds-muted">Primary Company</dt><dd className="mt-1">{item.companyName}</dd></div><div><dt className="text-vds-muted">Department</dt><dd className="mt-1">{item.department||"Not recorded"}</dd></div><div><dt className="text-vds-muted">Owner</dt><dd className="mt-1">{item.ownerName}</dd></div><div><dt className="text-vds-muted">Latest Update</dt><dd className="mt-1">{new Date(item.updatedAt).toLocaleDateString()}</dd></div></dl></Link>):<SmartEmptyState className="col-span-full" title="No Contacts" description="Import contacts or create a lead to begin building customer relationships." primaryLabel="Import Contacts" primaryHref="/onboarding?setup=crm" secondaryActions={[{label:"Create Lead",href:"/vayon/leads/new"}]}/>}</div>}
