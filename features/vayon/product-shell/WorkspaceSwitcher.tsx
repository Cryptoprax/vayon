"use client";
import { Button } from "@/features/platform/design-system";
import Image from "next/image";
import { Check, ChevronDown, Plus } from "lucide-react";
import { useEffect, useRef, useState } from "react";

export function WorkspaceSwitcher({name,logo,description}:{readonly name:string;readonly logo?:string;readonly description?:string}){const[open,setOpen]=useState(false),root=useRef<HTMLDivElement>(null);useEffect(()=>{function close(event:MouseEvent){if(!root.current?.contains(event.target as Node))setOpen(false)}document.addEventListener("mousedown",close);return()=>document.removeEventListener("mousedown",close)},[]);const initials=name.split(/\s+/).filter(Boolean).slice(0,2).map(part=>part[0]).join("").toUpperCase()||"V";const mark=logo?<Image src={logo} width={24} height={24} alt="" className="size-6 rounded-lg"/>:<span aria-label={`${name} workspace avatar`} className="grid size-6 shrink-0 place-items-center rounded-lg bg-gradient-to-br from-vds-primary to-vds-accent text-[9px] font-semibold text-vds-on-accent shadow-sm">{initials}</span>;return <div ref={root} className="relative shrink-0"><Button variant="control" type="button" onClick={()=>setOpen(value=>!value)} aria-haspopup="menu" aria-expanded={open} className="flex h-10 w-52 max-w-[40vw] shrink-0 items-center gap-2 rounded-xl border border-vds-border bg-vds-input px-3 text-left hover:border-vds-border-strong hover:bg-vds-hover">{mark}<span className="min-w-0 flex-1 truncate text-xs font-medium text-vds-secondary">{name}</span><ChevronDown className="size-4 text-vds-subtle"/></Button>{open&&<div role="menu" className="vds-dropdown-enter absolute left-0 top-12 z-[70] w-72 rounded-2xl border border-vds-border bg-vds-elevated p-2 shadow-xl shadow-vds-shadow"><p className="px-3 py-2 text-[10px] font-semibold uppercase tracking-[.15em] text-vds-subtle">Current workspace</p><div role="menuitem" className="flex items-center gap-3 rounded-xl bg-vds-primary-soft px-3 py-3 text-sm text-vds-secondary">{mark}<span className="min-w-0 flex-1 truncate">{name}</span><Check className="size-4 text-vds-primary"/></div>{description&&<p className="px-3 py-3 text-xs leading-5 text-vds-muted">{description}</p>}<p className="px-3 pb-1 pt-4 text-[10px] font-semibold uppercase tracking-[.15em] text-vds-subtle">Recent workspaces</p><p className="px-3 py-3 text-xs text-vds-muted">No recent workspaces.</p><Button variant="control" type="button" disabled className="flex w-full cursor-not-allowed items-center gap-3 rounded-xl px-3 py-3 text-sm text-vds-disabled"><Plus className="size-4"/>Create workspace<span className="ml-auto text-[9px] uppercase">Disabled</span></Button></div>}</div>}
