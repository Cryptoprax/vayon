import { WorkspaceContent } from "@/features/platform/design-system/layout/WorkspaceLayouts";
import{Badge,ButtonLink}from"@/features/platform/design-system";import type{ExecutionSession}from"../domain/contracts";const states=["running","queued","waiting","failed"]as const;export function WorkflowRuntimeDashboard({sessions}:{sessions:readonly ExecutionSession[]}){return <WorkspaceContent ><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Vayon automation</p><h1 className="mt-2 text-3xl font-semibold">Workflow Runtime</h1><p className="mt-2 text-sm text-vds-muted">Governed execution sessions, approvals, checkpoints, retries, and immutable history.</p></div><ButtonLink href="/vayon/workflows" variant="outline">Workflow designer</ButtonLink></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{states.map(state=><section key={state} className="rounded-xl border border-vds-border bg-vds-surface p-4"><span className="text-sm capitalize text-vds-muted">{state}</span><strong className="mt-2 block text-2xl">{sessions.filter(item=>item.state===state).length}</strong></section>)}</div><section className="mt-6 rounded-2xl border border-vds-border bg-vds-surface p-5"><h2 className="font-semibold">Execution history</h2>{sessions.length?<div className="mt-4 space-y-3">{sessions.map(session=><article key={session.id} className="rounded-xl border border-vds-border p-4"><div className="flex items-center justify-between gap-3"><strong>{session.plan.workflowId} · v{session.plan.workflowVersion}</strong><Badge>{session.state}</Badge></div><p className="mt-2 text-xs text-vds-muted">Session {session.id} · Updated {new Date(session.updatedAt).toLocaleString()}</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-vds-elevated"><div className="h-full bg-vds-primary" style={{width:`${Math.round(Object.keys(session.outputs).length/Math.max(1,session.plan.steps.length)*100)}%`}}/></div>{session.failure&&<p role="alert" className="mt-3 text-sm text-vds-danger">{session.failure.code}: {session.failure.message}</p>}</article>)}</div>:<div className="mt-4 rounded-xl border border-dashed border-vds-border p-10 text-center"><h3 className="font-semibold">No workflow executions yet</h3><p className="mt-2 text-sm text-vds-muted">Publish an approved workflow and invoke a manual or scheduled trigger. No execution history has been fabricated.</p></div>}</section><section className="mt-5 grid gap-4 lg:grid-cols-2"><div className="rounded-xl border border-vds-border p-5"><h2 className="font-semibold">Waiting approvals</h2><p className="mt-2 text-sm text-vds-muted">{sessions.filter(item=>item.state==="waiting").length?"Approval-governed sessions require an authorized decision.":"No approvals are waiting."}</p></div><div className="rounded-xl border border-vds-border p-5"><h2 className="font-semibold">Execution timeline</h2><p className="mt-2 text-sm text-vds-muted">Select a persisted session when durable runtime storage is enabled to inspect immutable audit entries.</p></div></section></WorkspaceContent>}
