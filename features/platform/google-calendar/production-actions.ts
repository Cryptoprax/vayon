"use server";import{redirect}from"next/navigation";import{revalidatePath}from"next/cache";import{CalendarProductionSyncService,type CalendarPermissions}from"./services/calendar-production-sync.service";
const back=(kind:"success"|"error",message:string)=>redirect(`/vayon/calendar?${kind}=${encodeURIComponent(message)}`);
export async function synchronizeGoogleCalendarAction(){try{const value=await new CalendarProductionSyncService().synchronize();revalidatePath("/vayon/calendar");revalidatePath("/vayon/home");back("success",`Calendar synchronized: ${value.imported} new, ${value.updated} updated, ${value.duplicates} unchanged, ${value.linked} linked to CRM.`)}catch(reason){back("error",reason instanceof Error?reason.message:"Calendar synchronization failed. Retry or reconnect Google Calendar.")}}
export async function updateGoogleCalendarPermissionsAction(form:FormData){const on=(key:keyof CalendarPermissions)=>form.get(key)==="on";try{await new CalendarProductionSyncService().permissions({readCalendar:on("readCalendar"),createEvents:on("createEvents"),updateEvents:on("updateEvents"),meetingSuggestions:on("meetingSuggestions"),reminderSuggestions:on("reminderSuggestions")});revalidatePath("/vayon/calendar");back("success","Calendar permissions updated.")}catch{back("error","Calendar permissions could not be updated. Try again or contact support.")}}
