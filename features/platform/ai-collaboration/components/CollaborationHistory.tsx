import type{CollaborationDashboard}from"../types";import{employeeName}from"./collaboration-view";
export default function CollaborationHistory({data}:{readonly data:CollaborationDashboard}){return <section className="rounded-[2rem] border border-vds-border bg-vds-surface/90 p-6 backdrop-blur sm:p-8" aria-labelledby="history-title"><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Collaboration History</p><h2 id="history-title" className="mt-2 text-2xl font-semibold">Live Collaboration Feed</h2>{data.timeline.length?<ol className="mt-6 space-y-4">{data.timeline.slice(0,30).map(event=><li className="relative border-l border-vds-accent-border pl-5" key={event.id}><i className="absolute -left-1 top-1.5 size-2 rounded-full bg-vds-primary"/><p className="text-sm"><b>{employeeName(event.agent)}</b> · {event.summary}</p><time className="mt-1 block text-xs text-vds-muted">{new Date(event.occurredAt).toLocaleString()}</time></li>)}</ol>:<p className="mt-6 rounded-2xl border border-dashed border-vds-border p-6 text-sm text-vds-muted">No collaboration has occurred yet.</p>}</section>}
