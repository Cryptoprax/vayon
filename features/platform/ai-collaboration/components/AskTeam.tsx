"use client";import{useState}from"react";import{Button}from"@/features/platform/design-system";
export function AskTeam(){const[question,setQuestion]=useState(""),[answer,setAnswer]=useState<string|null>(null);function recommend(){const q=question.toLowerCase(),person=/campaign|marketing|creative|brand/.test(q)?"Alex":/schedule|viewing|calendar|inspection|task/.test(q)?"David":/property|matching|listing|neighborhood/.test(q)?"Emma":/complaint|customer|support|review|retention/.test(q)?"Olivia":"Sarah";setAnswer(`${person} is the recommended teammate. Review the request before assigning any work.`)}return <section className="rounded-[2rem] border border-vds-border bg-vds-surface/90 p-6 backdrop-blur sm:p-8" aria-labelledby="ask-team-title"><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Ask the Team</p><h2 id="ask-team-title" className="mt-2 text-2xl font-semibold">My Team</h2><p className="mt-2 text-sm text-vds-muted">Describe what you need. VAYON recommends the right teammate without executing work.</p><div className="mt-5 flex flex-col gap-3 sm:flex-row"><input className="focus-ring min-h-11 flex-1 rounded-xl border border-vds-border bg-vds-input px-4 text-sm" aria-label="Ask My Team" placeholder="Find buyers, create a campaign, schedule a viewing…" value={question} onChange={event=>setQuestion(event.target.value)}/><Button type="button" onClick={recommend} disabled={!question.trim()}>Recommend teammate</Button></div>{answer&&<p className="mt-4 rounded-2xl bg-vds-primary-soft p-4 text-sm text-vds-primary" role="status">{answer}</p>}</section>}
