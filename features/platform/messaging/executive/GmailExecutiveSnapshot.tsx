import{GmailService}from"@/features/platform/integrations/google/services/gmail.service";
type Snapshot={status:"connected"|"not_connected"|"unavailable";awaiting?:number;urgent?:number;today?:number};
async function load():Promise<Snapshot>{const service=new GmailService();try{if(await service.status()!=="connected")return{status:"not_connected"};const[awaiting,urgent,today]=await Promise.all([service.list("inbox","-from:me"),service.list("important","is:unread"),service.list("inbox","newer_than:1d")]);return{status:"connected",awaiting:awaiting.resultSizeEstimate,urgent:urgent.resultSizeEstimate,today:today.resultSizeEstimate}}catch{return{status:"unavailable"}}}
export async function GmailExecutiveSnapshot(){const data=await load();if(data.status!=="connected")return <section className="rounded-2xl border border-vds-border p-5"><h2 className="font-semibold">Communication snapshot</h2><p className="mt-2 text-sm text-vds-muted">{data.status==="not_connected"?"Connect Gmail to include verified email activity in your morning briefing.":"Gmail data is temporarily unavailable. Retry or reconnect from Connected Apps."}</p></section>;return <section className="rounded-2xl border border-vds-border p-5"><h2 className="font-semibold">Email briefing</h2><dl className="mt-4 grid grid-cols-2 gap-4 text-sm sm:grid-cols-4"><div><dt className="text-vds-subtle">Unread priority emails</dt><dd className="mt-1 text-xl font-semibold">{data.urgent}</dd></div><div><dt className="text-vds-subtle">Customers awaiting replies</dt><dd className="mt-1 text-xl font-semibold">{data.awaiting}</dd></div><div><dt className="text-vds-subtle">Today&apos;s emails</dt><dd className="mt-1 text-xl font-semibold">{data.today}</dd></div><div><dt className="text-vds-subtle">Suggested follow-ups</dt><dd className="mt-1 text-xl font-semibold">{data.awaiting}</dd></div></dl><p className="mt-3 text-xs text-vds-subtle">Evidence: live Gmail label and query counts. Suggested follow-ups represent received inbox conversations awaiting review; no message is sent automatically.</p></section>}
