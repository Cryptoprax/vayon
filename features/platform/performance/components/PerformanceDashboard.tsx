import { WorkspaceContent, WorkspaceHeader } from "@/features/platform/design-system/layout/WorkspaceLayouts";
import type{PerformanceSnapshot}from"../contracts";const card="rounded-2xl border border-vds-border bg-vds-surface p-5";
export function PerformanceDashboard({data}:{data:PerformanceSnapshot}){return <WorkspaceContent ><WorkspaceHeader><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Enterprise performance</p><h1 className="mt-2 text-3xl font-semibold">Performance Dashboard</h1><p className="mt-2 text-sm text-vds-muted">Evidence-based latency, cache effectiveness, memory, frontend budgets, and regression status.</p></WorkspaceHeader><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{data.metrics.map(x=><article className={card} key={x.name}><p className="text-xs text-vds-muted">{x.name}</p><p className="mt-2 text-2xl font-semibold">{x.value==null?"Unavailable":`${x.value.toLocaleString()} ${x.unit}`}</p><p className="mt-2 text-xs uppercase">{x.state.replaceAll("_"," ")}{x.budget!=null?` · budget ${x.budget.toLocaleString()} ${x.unit}`:""}</p></article>)}</section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[["Cache hit ratio",data.cache.hitRatio==null?"Unavailable":`${(data.cache.hitRatio*100).toFixed(1)}%`],["Slow queries",String(data.slowQueries)],["Slow endpoints",String(data.slowEndpoints)],["Memory usage",`${data.memoryMb} MB`]].map(([name,value])=><article className={card} key={name}><h2 className="text-sm text-vds-muted">{name}</h2><p className="mt-2 text-xl font-semibold">{value}</p></article>)}</section><section className={card}><h2 className="font-semibold">Cache operations</h2><p className="mt-2 text-sm text-vds-muted">Provider {data.cache.provider} · {data.cache.hits} hits · {data.cache.misses} misses · {data.cache.writes} writes · {data.cache.evictions} invalidations. Edge and future Redis providers share the same tenant-safe contract.</p></section></WorkspaceContent>}
