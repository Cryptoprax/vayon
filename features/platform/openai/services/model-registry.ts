import type{CostEstimate,OpenAICapability,OpenAIModelId}from"../domain/models";interface ModelDefinition{readonly id:OpenAIModelId;readonly version:string;readonly inputPerMillion:number;readonly outputPerMillion:number;readonly maximumOutputTokens:number;readonly capabilities:readonly OpenAICapability["id"][]}
const all:readonly OpenAICapability["id"][]=["chat","responses","embeddings","summarize","recommend","classify","extract","moderate","countTokens","estimateCost","streaming"];
export const openAIModels:readonly ModelDefinition[]=Object.freeze([{id:"gpt-5",version:"gpt-5-2025-08-07",inputPerMillion:1.25,outputPerMillion:10,maximumOutputTokens:128000,capabilities:all},{id:"gpt-5.5",version:"gpt-5.5-2026-04-23",inputPerMillion:5,outputPerMillion:30,maximumOutputTokens:128000,capabilities:all}]);
export class OpenAIModelRegistry{resolve(id=process.env.OPENAI_MODEL??"gpt-5.5"):ModelDefinition{return openAIModels.find(x=>x.id===id)??{id,version:id,inputPerMillion:Number(process.env.OPENAI_INPUT_COST_PER_MILLION??0),outputPerMillion:Number(process.env.OPENAI_OUTPUT_COST_PER_MILLION??0),maximumOutputTokens:Number(process.env.OPENAI_MAX_OUTPUT_TOKENS??8192),capabilities:all}}capabilities(id?:string):readonly OpenAICapability[]{const model=this.resolve(id);return all.map(capability=>({id:capability,available:model.capabilities.includes(capability)}))}estimate(modelId:string,input:number,output:number):CostEstimate{const model=this.resolve(modelId),inputUsd=input*model.inputPerMillion/1_000_000,outputUsd=output*model.outputPerMillion/1_000_000;return{model:String(model.id),inputUsd,outputUsd,totalUsd:inputUsd+outputUsd,estimated:true,pricingVersion:"2026-08-15"}}}
