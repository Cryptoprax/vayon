import { WorkspaceContent, WorkspaceHeader } from "@/features/platform/design-system/layout/WorkspaceLayouts";
import type{AnalyticsSection,EnterpriseAnalyticsSnapshot}from"../contracts";const card="rounded-2xl border border-vds-border bg-vds-surface p-5";export function EnterpriseAnalyticsDashboard({data}:{data:EnterpriseAnalyticsSnapshot}){const sections=[data.acquisition,data.funnel,data.adoption,data.success,data.ai,data.roi,data.conversion,data.performance];return <WorkspaceContent ><WorkspaceHeader><p className="text-xs font-semibold uppercase tracking-[.2em] text-vds-primary">Enterprise analytics</p><h1 className="mt-2 text-3xl font-semibold">Acquisition, conversion, adoption & AI</h1><p className="mt-2 max-w-3xl text-sm text-vds-muted">Privacy-safe public analytics and tenant-scoped product evidence. Missing signals remain explicitly unavailable.</p></WorkspaceHeader><section className="mt-6 grid gap-3 sm:grid-cols-3"><article className={card}><p className="text-xs text-vds-muted">Analytics health</p><p className="mt-2 text-xl font-semibold capitalize">{data.health.state}</p></article><article className={card}><p className="text-xs text-vds-muted">Tracking failures</p><p className="mt-2 text-xl font-semibold">{data.health.trackingFailures}</p></article><article className={card}><p className="text-xs text-vds-muted">Missing events</p><p className="mt-2 text-xl font-semibold">{data.health.missingEvents.length}</p></article></section>{sections.map(section=><Section key={section.title} section={section}/>)}</WorkspaceContent>}
function Section({section}:{section:AnalyticsSection}){return <section className="mt-8"><h2 className="text-xl font-semibold">{section.title}</h2><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{section.metrics.map(metric=><article className={card} key={metric.label}><p className="text-xs text-vds-muted">{metric.label}</p><p className="mt-2 text-2xl font-semibold">{metric.available?format(metric.value,metric.unit):"Unavailable"}</p><p className="mt-2 text-xs text-vds-subtle">{metric.available?`Source: ${metric.source}`:metric.reason??"No authoritative event recorded."}</p></article>)}</div></section>}
function format(value:number|null,unit:string){if(value===null)return"Unavailable";if(unit==="percent")return`${value.toFixed(1)}%`;if(unit==="milliseconds")return`${value.toFixed(0)} ms`;if(unit==="currency")return new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);return value.toLocaleString("en-IN")}
