import { Button } from "@/features/platform/design-system";
import type { GmailSyncState } from "../services/gmail-production-sync.service";
import { synchronizeGmailAction,updateGmailPermissionsAction } from "../production-actions";

const labels=[['readEmail','Read Email'],['sendEmail','Send Email'],['draftReplies','Draft Replies'],['crmSynchronization','CRM Synchronization'],['threadSummaries','Thread Summaries']] as const;
export function GmailProductionControls({state}:{state:GmailSyncState}){return <section aria-labelledby="gmail-production-heading" className="rounded-2xl border border-vds-border bg-vds-surface p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-vds-primary">Production connection</p><h2 id="gmail-production-heading" className="mt-2 text-lg font-semibold">Gmail synchronization</h2><p className="mt-1 text-sm text-vds-muted">{state.account?`Connected as ${state.account}`:"Gmail is not connected."}</p><p className="mt-1 text-xs text-vds-subtle">Last sync: {state.lastSync?new Date(state.lastSync).toLocaleString():"Not synchronized yet"}</p></div><form action={synchronizeGmailAction}><Button disabled={!state.connected} type="submit">Synchronize now</Button></form></div><form action={updateGmailPermissionsAction} className="mt-5"><fieldset><legend className="text-sm font-medium">Permissions</legend><p className="mt-1 text-xs text-vds-subtle">AI suggestions remain draft-only. VAYON never sends email without your approval.</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{labels.map(([name,label])=><label key={name} className="flex min-h-11 items-center justify-between gap-3 rounded-xl border border-vds-border px-3 text-sm"><span>{label}</span><input type="checkbox" name={name} defaultChecked={state.permissions[name]} className="size-4 accent-vds-primary"/></label>)}</div><Button type="submit" variant="outline" className="mt-4">Save permissions</Button></fieldset></form></section>}
