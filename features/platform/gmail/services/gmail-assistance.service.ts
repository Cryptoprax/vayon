import type { MailMessage, MailThread } from "../domain/models";
export type GmailTeamMember="Sarah"|"Emma"|"Alex"|"David"|"Olivia";
export interface GmailAssistance{readonly owner:GmailTeamMember;readonly signal:string;readonly summary:string;readonly priority:"Normal"|"Review soon";readonly suggestions:readonly string[];readonly draftOpening:string}
const includes=(text:string,words:readonly string[])=>words.some(word=>text.includes(word));
export function analyzeGmailThread(message:MailMessage,thread:MailThread):GmailAssistance{const evidence=`${message.subject} ${message.snippet} ${message.plainText}`.toLowerCase();let owner:GmailTeamMember="Sarah",signal="Customer conversation";if(includes(evidence,["viewing","property","bedroom","villa","apartment","listing"])){owner="Emma";signal="Property or viewing request"}else if(includes(evidence,["campaign","newsletter","advert","marketing","source"])){owner="Alex";signal="Marketing response"}else if(includes(evidence,["schedule","meeting","calendar","appointment","operations"])){owner="David";signal="Scheduling or operational request"}else if(includes(evidence,["support","help","issue","problem","unhappy","complaint"])){owner="Olivia";signal="Support or satisfaction signal"}else if(includes(evidence,["buy","buyer","sell","seller","price","offer","follow up"]))signal="Buyer, seller, or follow-up opportunity";const priority=message.labels.includes("IMPORTANT")||message.labels.includes("UNREAD")&&includes(evidence,["urgent","today","asap","complaint"])?"Review soon":"Normal",suggestions=["Review the linked CRM contact before replying.",includes(evidence,["meeting","viewing","appointment","schedule"])?"Confirm availability and propose an appointment.":"Draft a helpful reply for user approval.","Create a follow-up task if the conversation remains unresolved."];return Object.freeze({owner,signal,priority,summary:`${thread.messages.length} message${thread.messages.length===1?"":"s"} in “${message.subject||"Email conversation"}”. Latest evidence is from ${message.sender.email}.`,suggestions:Object.freeze(suggestions),draftOpening:`Hello ${message.sender.name||"there"},\n\nThank you for your email about ${message.subject||"your request"}.`})}
