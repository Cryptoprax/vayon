import type { ReactNode } from "react";
import { Card } from "../core/Surfaces";
export function KpiCard({ label, value, trend, icon }: { readonly label: string; readonly value?: string; readonly trend?: string; readonly icon?: ReactNode }) { return <Card className="min-h-40"><div className="flex items-center gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-vds-primary-soft text-[var(--vds-color-primary)] [&_svg]:size-5">{icon}</span><p className="text-sm font-medium text-[var(--vds-color-muted)]">{label}</p></div><p className="mt-5 text-3xl font-semibold tracking-tight">{value ?? "—"}</p>{trend && <p className="mt-auto pt-3 text-xs font-medium text-[var(--vds-color-success)]">{trend}</p>}</Card> }
export function MetricCard({ label, value, description }: { readonly label: string; readonly value?: string; readonly description?: string }) { return <Card><p className="text-xs uppercase tracking-[.14em] text-[var(--vds-color-subtle)]">{label}</p><p className="mt-3 text-2xl font-semibold">{value ?? "—"}</p>{description && <p className="mt-2 text-xs text-[var(--vds-color-muted)]">{description}</p>}</Card> }
export function StatusCard({ title, status, description, tone = "neutral" }: { readonly title: string; readonly status: string; readonly description?: string; readonly tone?: "neutral" | "success" | "warning" | "danger" }) { const tones = { neutral: "bg-vds-subtle", success: "bg-vds-success", warning: "bg-vds-warning", danger: "bg-vds-danger" }; return <Card><div className="flex items-center gap-2"><span className={`size-2 rounded-full ${tones[tone]}`}/><h3 className="font-medium">{title}</h3><span className="ml-auto text-xs text-[var(--vds-color-muted)]">{status}</span></div>{description && <p className="mt-3 text-sm text-[var(--vds-color-muted)]">{description}</p>}</Card> }
export function TimelineCard({ title, timestamp, description, icon }: { readonly title: string; readonly timestamp: string; readonly description?: string; readonly icon?: ReactNode }) { return <Card className="relative ml-4"><span className="absolute -left-[1.3rem] top-6 size-2.5 rounded-full bg-[var(--vds-color-primary)] shadow-[0_0_12px_var(--vds-color-primary)]"/><div className="flex gap-3"><span className="text-[var(--vds-color-primary)]">{icon}</span><div><h3 className="text-sm font-medium">{title}</h3><time className="mt-1 block text-xs text-[var(--vds-color-subtle)]">{timestamp}</time>{description && <p className="mt-3 text-sm text-[var(--vds-color-muted)]">{description}</p>}</div></div></Card> }
