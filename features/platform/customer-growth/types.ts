import type { FounderKpi, PlatformMetricRow } from "@/features/platform/founder/types";

export type CustomerHealthClass = "Healthy" | "Needs Attention" | "At Risk" | "Critical";
export interface OrganizationHealth { readonly id: string; readonly name: string; readonly plan: string; readonly score: number; readonly classification: CustomerHealthClass; readonly confidence: number; readonly reasons: readonly string[]; readonly churnProbability: number; readonly riskFactors: readonly string[]; readonly recommendedActions: readonly string[]; readonly expectedExpansionValue: number | null; readonly expansionOpportunities: readonly string[]; }
export interface RenewalItem { readonly id: string; readonly organizationId: string; readonly status: string; readonly renewsAt: string | null; readonly autoRenew: boolean; readonly outstandingInvoices: number; readonly recommendation: string; }
export interface CustomerJourneyEvent { readonly id: string; readonly organizationId: string | null; readonly category: string; readonly title: string; readonly occurredAt: string; }
export interface CustomerGrowthSnapshot { readonly kpis: readonly FounderKpi[]; readonly metrics: readonly PlatformMetricRow[]; readonly organizations: readonly OrganizationHealth[]; readonly renewals: readonly RenewalItem[]; readonly journey: readonly CustomerJourneyEvent[]; readonly automations: readonly { id: string; name: string; trigger: string; consentRequired: true; unsubscribeRequired: true; approvalRequired: true }[]; readonly reports: readonly { id: string; label: string; formats: readonly ["PDF", "PowerPoint"] }[]; readonly generatedAt: string; }
