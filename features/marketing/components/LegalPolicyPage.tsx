import Link from "next/link";
export interface PolicySection { readonly title: string; readonly paragraphs: readonly string[] }
export function LegalPolicyPage({ title, description, sections }: { title: string; description: string; sections: readonly PolicySection[] }) {
  return <main className="mx-auto max-w-4xl px-5 py-20 sm:px-8"><p className="text-xs font-semibold uppercase tracking-[.22em] text-vds-primary">Vayon legal</p><h1 className="mt-4 text-4xl font-semibold tracking-tight sm:text-6xl">{title}</h1><p className="mt-5 max-w-3xl text-lg text-vds-muted">{description}</p><p className="mt-4 text-xs text-vds-subtle">Last reviewed: 23 August 2026</p><div className="mt-12 space-y-8">{sections.map(section=><section className="rounded-2xl border border-vds-border bg-vds-surface p-6" key={section.title}><h2 className="text-xl font-semibold">{section.title}</h2>{section.paragraphs.map(paragraph=><p className="mt-3 leading-7 text-vds-muted" key={paragraph}>{paragraph}</p>)}</section>)}</div><p className="mt-10 text-sm text-vds-muted">Questions about this policy can be submitted through the <Link className="text-vds-primary underline" href="/contact">Vayon contact form</Link>.</p></main>;
}
